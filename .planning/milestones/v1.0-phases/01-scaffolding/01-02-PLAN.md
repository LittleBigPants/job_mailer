---
phase: 01-scaffolding
plan: 02
type: tdd
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/job_mailer/config.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "check_env() exits with a message naming missing keys — not a raw traceback"
    - "check_env() passes silently when all three keys are present in the environment"
    - "load_profile() returns a dict when profile.toml exists"
    - "load_profile() exits with a clear message when profile.toml is absent — not a KeyError"
    - "All 5 unit tests pass: pytest tests/test_config.py -x returns green"
  artifacts:
    - path: "src/job_mailer/config.py"
      provides: "load_dotenv() at module top, check_env(), load_profile()"
      exports: ["check_env", "load_profile"]
    - path: "tests/test_config.py"
      provides: "5 unit tests covering check_env and load_profile"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "monkeypatch fixtures for env vars and tmp_path for TOML files"
  key_links:
    - from: "src/job_mailer/config.py"
      to: "os.environ"
      via: "load_dotenv() at module top-level, then check_env() validates presence"
      pattern: "load_dotenv\\(\\)"
    - from: "tests/test_config.py"
      to: "src/job_mailer/config.py"
      via: "import check_env, load_profile"
      pattern: "from job_mailer.config import"
---

<objective>
Implement the startup config module using TDD: write failing tests first, then implement `check_env()` and `load_profile()` until all tests pass.

Purpose: INFRA-02 and INFRA-03 require provably correct behavior — `check_env()` must name missing keys, not traceback; `load_profile()` must handle a missing file gracefully. TDD makes these behaviors explicit before any code is written.
Output: `src/job_mailer/config.py` with `check_env()` and `load_profile()`, backed by 5 passing unit tests.
</objective>

<execution_context>
@/home/littlepants/.claude/get-shit-done/workflows/execute-plan.md
@/home/littlepants/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-VALIDATION.md
@.planning/phases/01-scaffolding/01-01-SUMMARY.md

<interfaces>
<!-- From 01-01-PLAN.md output — package structure created in Plan 01 -->
<!-- src/job_mailer/__init__.py exists (empty) -->
<!-- pyproject.toml has testpaths=["tests"], addopts="-x" -->
<!-- pytest installed via uv pip install -e ".[dev]" -->

Package entry point (src/job_mailer/__main__.py):
```python
import typer
app = typer.Typer()
```
</interfaces>
</context>

<feature>
  <name>Startup config validation (check_env + load_profile)</name>
  <files>src/job_mailer/config.py, tests/__init__.py, tests/conftest.py, tests/test_config.py</files>
  <behavior>
    RED phase — write these failing tests first:

    Test 1 (test_check_env_missing_key): monkeypatch removes GROQ_API_KEY from os.environ; calling check_env() raises SystemExit.

    Test 2 (test_check_env_all_present): monkeypatch sets all three keys (GROQ_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL) to non-empty strings; check_env() returns None without raising.

    Test 3 (test_check_env_names_missing_key): monkeypatch removes RESEND_API_KEY; calling check_env() raises SystemExit whose string representation contains "RESEND_API_KEY" — the key name, not a traceback.

    Test 4 (test_load_profile_schema): tmp_path fixture creates a minimal valid profile.toml with [developer] name = "Test"; load_profile(path) returns a dict with a "developer" key.

    Test 5 (test_load_profile_missing_file): load_profile(path) called with a path that does not exist raises SystemExit whose string representation contains "profile" — an actionable message, not a FileNotFoundError.

    GREEN phase — implement config.py to make all 5 tests pass:
    - load_dotenv() called at module top-level (before any function definitions)
    - check_env() iterates _REQUIRED_ENV_KEYS = ["GROQ_API_KEY", "RESEND_API_KEY", "RESEND_FROM_EMAIL"], collects missing, calls sys.exit() with a message listing them
    - load_profile(path: Path = _PROFILE_PATH) opens file in "rb" mode, calls tomllib.load(f), returns dict; calls sys.exit() with helpful message if path does not exist
  </behavior>
  <implementation>
    Use the exact patterns from RESEARCH.md "config.py — Full Startup Module" code example.

    Key constraints (from RESEARCH.md pitfalls):
    - load_dotenv() must be at MODULE TOP LEVEL, not inside a function — prevents "keys intermittently missing in tests" pitfall
    - Use sys.exit() not raise ValueError — caller gets a human message not a traceback
    - Use os.environ.get(k) not os.environ[k] inside check_env() — avoids KeyError before check runs
    - Open profile.toml in binary mode "rb" — tomllib.load() requires this

    tests/conftest.py should provide a fixture that clears/restores the three env var keys around each test, using monkeypatch.delenv(..., raising=False) to avoid test pollution.
  </implementation>
</feature>

<verification>
  <automated>cd /home/littlepants/Documents/repositories/job_mailer && python -m pytest tests/test_config.py -x -v</automated>
</verification>

<success_criteria>
- `pytest tests/test_config.py -x` exits 0 with 5 tests passing
- RED commit exists: `test(01-02): add failing tests for check_env and load_profile`
- GREEN commit exists: `feat(01-02): implement check_env and load_profile`
- `from job_mailer.config import check_env, load_profile` succeeds in the venv
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-02-SUMMARY.md` following the summary template.
</output>
