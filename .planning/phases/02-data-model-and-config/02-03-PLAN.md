---
phase: 02-data-model-and-config
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/job_mailer/config.py
autonomous: true
requirements:
  - INPUT-01
must_haves:
  truths:
    - "validate_profile() raises SystemExit with the missing field path in the message when any required field is absent"
    - "validate_profile() returns None without raising when all required fields are present"
    - "Missing fields are reported all at once in a single sys.exit() call, not one-by-one"
    - "All test_config.py tests (Phase 1 originals + new Phase 2 stubs) pass green"
  artifacts:
    - path: "src/job_mailer/config.py"
      provides: "Extended with validate_profile() and _REQUIRED_PROFILE_FIELDS; existing check_env() and load_profile() untouched"
      exports: ["check_env", "load_profile", "validate_profile"]
  key_links:
    - from: "src/job_mailer/__main__.py"
      to: "src/job_mailer/config.py"
      via: "from job_mailer.config import check_env, load_profile, validate_profile"
      pattern: "validate_profile"
---

<objective>
Extend src/job_mailer/config.py with validate_profile() — adds required-field validation on top of the existing TOML loader.

Purpose: Phase 1 load_profile() returns a raw dict with no field guarantees. Any downstream code accessing profile["developer"]["name"] would raise a cryptic KeyError if the field is missing. validate_profile() converts that into a clear sys.exit() message with all missing field paths listed at once.
Output: config.py with validate_profile() added. All test_config.py tests green.
</objective>

<execution_context>
@/home/littlepants/.claude/get-shit-done/workflows/execute-plan.md
@/home/littlepants/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-model-and-config/02-01-SUMMARY.md
@src/job_mailer/config.py
@tests/test_config.py
@profile.example.toml
</context>

<interfaces>
<!-- Existing config.py exports (from Phase 1). Must remain unchanged. -->
```python
from job_mailer.config import check_env   # raises SystemExit on missing env var
from job_mailer.config import load_profile  # raises SystemExit if profile.toml absent
```

<!-- New export required by Wave 0 test stubs and Plan 04: -->
```python
from job_mailer.config import validate_profile
# validate_profile(profile: dict) -> None
# Raises SystemExit listing all missing field dot-paths if any are absent.
# Returns None silently if all required fields present.
```

<!-- Required field paths (must match profile.example.toml schema): -->
_REQUIRED_PROFILE_FIELDS = [
    ("developer", "name"),
    ("developer", "title"),
    ("developer", "contact", "email"),
    ("developer", "skills", "primary"),
    ("developer", "skills", "specialisation"),
]

<!-- Minimal valid profile dict for test reference: -->
{
  "developer": {
    "name": "A",
    "title": "B",
    "contact": {"email": "a@b.com"},
    "skills": {"primary": "Python", "specialisation": "backend"}
  }
}
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validate_profile() to config.py</name>
  <files>src/job_mailer/config.py</files>
  <behavior>
    - validate_profile({}) raises SystemExit; the str() of the exception contains "developer.name"
    - validate_profile(minimal_valid_dict) returns None
    - validate_profile(dict_missing_two_fields) raises SystemExit once; both missing field paths appear in the message
    - Existing check_env() and load_profile() tests still pass (no modifications to existing functions)
  </behavior>
  <action>
    APPEND to the existing src/job_mailer/config.py (do NOT replace or rewrite it):

    1. After the existing imports, add nothing new — sys is already imported.

    2. After the existing load_profile() function, add:

       _REQUIRED_PROFILE_FIELDS: list[tuple[str, ...]] = [
           ("developer", "name"),
           ("developer", "title"),
           ("developer", "contact", "email"),
           ("developer", "skills", "primary"),
           ("developer", "skills", "specialisation"),
       ]

    3. Then add validate_profile():

       def validate_profile(profile: dict) -> None:
           """Raise SystemExit with actionable message if required profile.toml fields are missing.

           Always call this immediately after load_profile() before accessing any profile keys.
           """
           missing = []
           for path in _REQUIRED_PROFILE_FIELDS:
               node = profile
               for key in path:
                   if not isinstance(node, dict) or key not in node:
                       missing.append(".".join(path))
                       break
                   node = node[key]
           if missing:
               fields_list = "\n  ".join(missing)
               sys.exit(
                   f"Error: profile.toml is missing required fields:\n  {fields_list}\n"
                   f"\nSee profile.example.toml for all required fields."
               )

    CRITICAL: Do NOT touch check_env(), load_profile(), or any existing code above them.
    CRITICAL: _REQUIRED_PROFILE_FIELDS must match the fields documented in profile.example.toml.
    CRITICAL: validate_profile() must list ALL missing fields in one sys.exit() call, not stop at the first.
  </action>
  <verify>
    <automated>pytest tests/test_config.py -x</automated>
  </verify>
  <done>All test_config.py tests pass: four Phase 1 originals and two new Phase 2 validate_profile stubs. No regressions.</done>
</task>

</tasks>

<verification>
`pytest tests/test_config.py -x` — all six tests green (four original + two new).
`pytest tests/ -x` — no regressions across full suite.
</verification>

<success_criteria>
- validate_profile() added to config.py after load_profile()
- _REQUIRED_PROFILE_FIELDS defined as module-level constant
- Existing check_env() and load_profile() functions untouched
- All tests/test_config.py tests green (including Phase 1 originals)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-and-config/02-03-SUMMARY.md`
</output>
