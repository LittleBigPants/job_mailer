---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .env.example
  - src/job_mailer/__init__.py
  - README.md
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "Running `git status` after setup shows .env and profile.toml are not tracked"
    - "pyproject.toml defines all runtime dependencies and the job-mailer entry point"
    - "The src/job_mailer package directory exists and is importable"
    - ".env.example is committed and documents all three required keys"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, dependencies, entry point, dev deps, ruff config, pytest config"
      contains: "job-mailer"
    - path: ".gitignore"
      provides: "Prevents .env and profile.toml from ever being tracked"
      contains: ".env"
    - path: ".env.example"
      provides: "Documents GROQ_API_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL"
    - path: "src/job_mailer/__init__.py"
      provides: "Makes job_mailer a proper Python package"
  key_links:
    - from: ".gitignore"
      to: ".env"
      via: "gitignore entry"
      pattern: "^\\.env$"
    - from: "pyproject.toml"
      to: "src/job_mailer"
      via: "[project.scripts] entry point"
      pattern: "job_mailer.__main__"
---

<objective>
Bootstrap the project skeleton: package structure, dependency manifest, gitignore, and env example. This is the foundation every other plan builds on.

Purpose: All subsequent plans require an importable package, a dependency-pinned environment, and the guarantee that secrets cannot be accidentally committed. These cannot be added retroactively without risk.
Output: Working Python package with pyproject.toml, clean gitignore, and a committed .env.example.
</objective>

<execution_context>
@/home/littlepants/.claude/get-shit-done/workflows/execute-plan.md
@/home/littlepants/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton and gitignore</name>
  <files>pyproject.toml, .gitignore, src/job_mailer/__init__.py, src/job_mailer/__main__.py</files>
  <action>
Create the following files:

**pyproject.toml** at repo root — use the exact content from the RESEARCH.md "pyproject.toml — Minimal Project Scaffold" code example, which includes:
- `[project]` with name="job-mailer", version="0.1.0", requires-python=">=3.11"
- All runtime dependencies: typer>=0.24.1, httpx>=0.28.1, beautifulsoup4>=4.14.3, lxml>=6.0.2, groq>=1.1.1, resend>=2.23.0, tenacity>=9.1.4, python-dotenv>=1.0
- `[project.scripts]`: `job-mailer = "job_mailer.__main__:app"`
- `[project.optional-dependencies]` dev group: pytest, ruff
- `[build-system]` with hatchling
- `[tool.ruff]` with line-length=100, target-version="py311"
- `[tool.pytest.ini_options]` with testpaths=["tests"], addopts="-x"

**src/job_mailer/__init__.py** — empty file (creates the package).

**src/job_mailer/__main__.py** — minimal Typer stub so the entry point exists:
```python
import typer

app = typer.Typer()


@app.command()
def main() -> None:
    """job-mailer: CSV of company URLs -> personalized cold emails."""
    typer.echo("job-mailer: not yet implemented")


if __name__ == "__main__":
    app()
```

**.gitignore** — must be created BEFORE any other file that should be ignored. Include at minimum:
```
# Secrets and personal profile — never commit
.env
profile.toml

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/

# Runtime artifacts
*.log
```

After creating files, install the virtual environment and dependencies:
```bash
cd /home/littlepants/Documents/repositories/job_mailer
uv venv
uv pip install -e ".[dev]"
```

The `-e ".[dev]"` installs the package in editable mode with dev dependencies (pytest, ruff). This makes `import job_mailer` work immediately.
  </action>
  <verify>
    <automated>cd /home/littlepants/Documents/repositories/job_mailer && python -c "import job_mailer; print('package importable')" && git status --short | grep -v "^?" | grep "\.env" | wc -l | grep -q "^0$" && echo ".env not tracked" && python -m pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>pyproject.toml committed, src/job_mailer/__init__.py exists, .env is absent from `git status` tracked files, `python -c "import job_mailer"` succeeds in the venv.</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example</name>
  <files>.env.example</files>
  <action>
Create `.env.example` at the repo root documenting all three required environment variables:

```
# .env.example — committed to repo
# Copy this file to .env and fill in real values.
# .env is gitignored and must NEVER be committed.

GROQ_API_KEY=gsk_your_groq_api_key_here
RESEND_API_KEY=re_your_resend_api_key_here
RESEND_FROM_EMAIL=outreach@yourdomain.com
```

Important: do NOT create `.env` itself — only the example file. The `.gitignore` from Task 1 already excludes `.env`.

Verify the example file is tracked (committed) but `.env` is not:
```bash
git status --short .env.example   # should show as untracked (new file)
git status --short .env           # should show nothing — file does not exist
```
  </action>
  <verify>
    <automated>cd /home/littlepants/Documents/repositories/job_mailer && test -f .env.example && grep -c "GROQ_API_KEY\|RESEND_API_KEY\|RESEND_FROM_EMAIL" .env.example | grep -q "3" && echo "all 3 keys documented" && test ! -f .env && echo ".env absent"</automated>
  </verify>
  <done>.env.example exists with all three required key names documented. .env does not exist. .env is listed in .gitignore.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `git status --short | grep -v "^?" | grep "\.env"` returns empty — .env is untracked
2. `python -c "import job_mailer"` succeeds
3. `cat .gitignore | grep "\.env"` shows the entry
4. `cat .env.example | grep "GROQ_API_KEY"` shows the key name
5. `cat pyproject.toml | grep "job-mailer"` shows the entry point
</verification>

<success_criteria>
- pyproject.toml defines all runtime + dev dependencies and the CLI entry point
- .gitignore includes .env and profile.toml before those files are created
- src/job_mailer/ is an importable Python package
- .env.example documents all three required API key names
- No secrets exist in any committed file
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-01-SUMMARY.md` following the summary template.
</output>
