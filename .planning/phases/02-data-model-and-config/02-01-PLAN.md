---
phase: 02-data-model-and-config
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_models.py
  - tests/test_cli.py
  - tests/test_config.py
autonomous: true
requirements:
  - INPUT-01
must_haves:
  truths:
    - "tests/test_models.py exists with failing stubs for CompanyRecord and Status enum tests"
    - "tests/test_cli.py exists with failing stubs for --input flag and exit-clean tests"
    - "tests/test_config.py is extended with failing stubs for validate_profile() tests"
    - "pytest collects all new test stubs and reports them as errors (not import errors)"
  artifacts:
    - path: "tests/test_models.py"
      provides: "Failing test stubs for CompanyRecord instantiation and Status enum values"
      exports: ["test_company_record_defaults", "test_company_record_fields", "test_status_enum_values"]
    - path: "tests/test_cli.py"
      provides: "Failing test stubs for CLI --input argument and exit behavior"
      exports: ["test_cli_input_exits_clean", "test_cli_missing_input_flag"]
    - path: "tests/test_config.py"
      provides: "Extended with validate_profile() test stubs (appended, existing tests untouched)"
      exports: ["test_validate_profile_missing_field", "test_validate_profile_all_present"]
  key_links:
    - from: "tests/test_models.py"
      to: "src/job_mailer/models.py"
      via: "from job_mailer.models import CompanyRecord, Status"
      pattern: "from job_mailer\\.models import"
    - from: "tests/test_cli.py"
      to: "src/job_mailer/__main__.py"
      via: "typer.testing.CliRunner"
      pattern: "CliRunner"
    - from: "tests/test_config.py"
      to: "src/job_mailer/config.py"
      via: "from job_mailer.config import validate_profile"
      pattern: "from job_mailer\\.config import.*validate_profile"
---

<objective>
Create Wave 0 test scaffolds for all Phase 2 behaviors before any production code exists.

Purpose: Nyquist compliance — every implementation task in Plans 02, 03, 04 must have a pre-existing test that starts RED and turns GREEN when the production code is written. Writing tests first forces the contracts to be explicit before any implementation begins.
Output: Three test files with failing stubs (import errors expected until Wave 1 code exists).
</objective>

<execution_context>
@/home/littlepants/.claude/get-shit-done/workflows/execute-plan.md
@/home/littlepants/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/conftest.py
@tests/test_config.py
</context>

<feature>
  <name>Wave 0 — Test scaffolds for Phase 2</name>
  <files>tests/test_models.py, tests/test_cli.py, tests/test_config.py</files>
  <behavior>
    test_models.py:
    - test_company_record_defaults: CompanyRecord("https://example.com") instantiates without error; company_name == ""; email_found == ""; generated_message == ""; status == Status.PENDING; resend_message_id == ""; timestamp is a non-empty string
    - test_company_record_fields: CompanyRecord has exactly these attributes: url, company_name, email_found, generated_message, status, resend_message_id, timestamp
    - test_status_enum_values: Status enum has exactly these .value strings: "pending", "sent", "no_email_found", "generation_failed", "send_failed", "skipped"

    test_config.py (appended stubs — do NOT modify existing tests):
    - test_validate_profile_missing_field: validate_profile({}) raises SystemExit; the error message contains the name of a missing field (e.g. "developer.name")
    - test_validate_profile_all_present: validate_profile(profile_dict_with_all_required_fields) returns None without raising; use a minimal valid dict that satisfies all required paths

    test_cli.py:
    - test_cli_input_exits_clean: invoking the CLI with --input pointing to a real temp CSV file, with env vars set, exits with code 0
    - test_cli_missing_input_flag: invoking the CLI with no arguments exits with non-zero code
  </behavior>
  <implementation>
    Write each test stub so it:
    1. Imports the symbol under test (will cause ImportError/AttributeError until Wave 1 code exists — that is correct RED state)
    2. Has a meaningful assertion that documents expected behavior
    3. Uses pytest.raises(SystemExit) for error cases

    For test_cli.py use typer.testing.CliRunner to invoke the app object imported from job_mailer.__main__.

    For the test_validate_profile_all_present stub, the minimal valid profile dict must satisfy all paths in _REQUIRED_PROFILE_FIELDS:
    {"developer": {"name": "A", "title": "B", "contact": {"email": "a@b.com"}, "skills": {"primary": "Python", "specialisation": "backend"}}}

    Do NOT stub the tests as pass/xfail. They must actively import the not-yet-existing symbols so that plan execution order is enforced by test failures.

    Append the validate_profile stubs to the END of the existing tests/test_config.py. Add a blank line and a comment separator:
    # ---------------------------------------------------------------------------
    # validate_profile() tests — added Phase 2
    # ---------------------------------------------------------------------------
  </implementation>
</feature>

<verification>
After writing all three files:
- `pytest tests/test_models.py -x` — should fail with ImportError (models.py not yet created). CORRECT RED state.
- `pytest tests/test_cli.py -x` — should fail with ImportError or test failure. CORRECT RED state.
- `pytest tests/test_config.py::test_validate_profile_missing_field -x` — should fail with ImportError (validate_profile not yet in config.py). CORRECT RED state.
- `pytest tests/test_config.py -x` — the four existing Phase 1 tests must still PASS. No regressions.

If any existing test_config.py test fails after the append, the append introduced a problem — fix it before committing.
</verification>

<success_criteria>
- tests/test_models.py exists with test_company_record_defaults, test_company_record_fields, test_status_enum_values
- tests/test_cli.py exists with test_cli_input_exits_clean, test_cli_missing_input_flag
- tests/test_config.py has four original Phase 1 tests still passing AND two new validate_profile stubs that fail with ImportError
- No watch-mode flags used in any verify command
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-and-config/02-01-SUMMARY.md`
</output>
