---
phase: 02-data-model-and-config
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/job_mailer/models.py
autonomous: true
requirements:
  - INPUT-01
must_haves:
  truths:
    - "CompanyRecord can be instantiated with only url; all other fields have documented defaults"
    - "Status enum has all six pipeline status values as typed string constants"
    - "CompanyRecord.to_csv_row() returns a flat dict with all seven LOG-02 fields using Status.value for status"
    - "All test_models.py tests pass green"
  artifacts:
    - path: "src/job_mailer/models.py"
      provides: "CompanyRecord dataclass and Status string enum"
      exports: ["CompanyRecord", "Status"]
      min_lines: 30
  key_links:
    - from: "src/job_mailer/models.py"
      to: "tests/test_models.py"
      via: "from job_mailer.models import CompanyRecord, Status"
      pattern: "from job_mailer\\.models import"
---

<objective>
Create src/job_mailer/models.py with CompanyRecord dataclass and Status string enum — the shared row model used by every downstream phase.

Purpose: Every pipeline stage (scraper, generator, sender, logger) operates on CompanyRecord instances. Defining the contract here prevents string-keyed dict bugs in all downstream phases.
Output: src/job_mailer/models.py with CompanyRecord and Status exported. All test_models.py tests green.
</objective>

<execution_context>
@/home/littlepants/.claude/get-shit-done/workflows/execute-plan.md
@/home/littlepants/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-model-and-config/02-01-SUMMARY.md
@tests/test_models.py
</context>

<interfaces>
<!-- Contracts this plan must satisfy. From tests/test_models.py (Wave 0). -->
<!-- Executor uses these directly — no codebase exploration needed. -->

Required exports (from Wave 0 test stubs):
```python
from job_mailer.models import CompanyRecord, Status
```

CompanyRecord fields (all seven, matching LOG-02):
- url: str                     # required, no default
- company_name: str = ""
- email_found: str = ""
- generated_message: str = ""
- status: Status = Status.PENDING
- resend_message_id: str = ""
- timestamp: str               # default_factory, NOT class-level default

Status values (six total):
- PENDING = "pending"
- SENT = "sent"
- NO_EMAIL_FOUND = "no_email_found"
- GENERATION_FAILED = "generation_failed"
- SEND_FAILED = "send_failed"
- SKIPPED = "skipped"

to_csv_row() -> dict[str, str]:
  Returns all seven fields; status serialised as status.value (the string, not the enum object).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/job_mailer/models.py</name>
  <files>src/job_mailer/models.py</files>
  <behavior>
    - Status("sent") == "sent" is True (str mixin)
    - CompanyRecord("https://example.com").status == Status.PENDING
    - CompanyRecord("https://example.com").timestamp is a non-empty string (computed fresh per instance, not shared)
    - Two separately-constructed CompanyRecord instances have different timestamps (proves default_factory)
    - to_csv_row() returns {"status": "pending", ...} not {"status": "Status.PENDING", ...}
  </behavior>
  <action>
    Create src/job_mailer/models.py with:

    1. Module docstring: "Shared data model for the job-mailer pipeline."

    2. Status class — inherit from both str and enum.Enum so Status.SENT == "sent" is True without calling .value:
       class Status(str, enum.Enum):
           PENDING = "pending"
           SENT = "sent"
           NO_EMAIL_FOUND = "no_email_found"
           GENERATION_FAILED = "generation_failed"
           SEND_FAILED = "send_failed"
           SKIPPED = "skipped"

    3. CompanyRecord dataclass — all seven fields in order:
       - url: str  (required, no default)
       - company_name: str = ""
       - email_found: str = ""
       - generated_message: str = ""
       - status: Status = Status.PENDING
       - resend_message_id: str = ""
       - timestamp: str = field(default_factory=lambda: datetime.now(timezone.utc).isoformat())
         NOTE: use datetime.now(timezone.utc) NOT deprecated datetime.utcnow()

    4. to_csv_row() method returns dict[str, str] with all seven keys; use self.status.value for status field.

    Imports needed: from __future__ import annotations; import enum; from dataclasses import dataclass, field; from datetime import datetime, timezone

    CRITICAL: models.py must NOT import anything from job_mailer package (no circular imports).
    CRITICAL: timestamp must use field(default_factory=...) not a bare class-level expression.
  </action>
  <verify>
    <automated>pytest tests/test_models.py -x</automated>
  </verify>
  <done>All three test_models.py tests pass. pytest tests/ -x shows no regressions in test_config.py.</done>
</task>

</tasks>

<verification>
`pytest tests/test_models.py -x` — all three tests green.
`pytest tests/ -x` — no regressions in existing Phase 1 tests.
</verification>

<success_criteria>
- src/job_mailer/models.py created with CompanyRecord and Status exported
- Status is a str-enum so Status.SENT == "sent" is True
- timestamp uses default_factory (not class-level default)
- to_csv_row() serialises status as .value string
- All tests/test_models.py tests green
- No regressions in tests/test_config.py
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-and-config/02-02-SUMMARY.md`
</output>
